name = "wsdl_rewrite_single"
operation = "wsdl"
http_method = "GET"
http_path = "/soap"
content_type = "text/xml"
soap_version = "1.2"
expected_status = 200
outcome = "success"
# request_file is ignored for GET requests but must be present per schema
request_file = "wsdl_get.request.xml"
